(()=>{'use strict';const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function init(){const host=document.querySelector('#lawsList');try{const r=await fetch('data/laws-files.json?v=20260907-2',{cache:'no-store'});if(!r.ok)throw 0;const d=await r.json();document.querySelector('#lawsCount').textContent=`${d.files.length} ملفات`;host.innerHTML=d.files.map((x,i)=>{const url=`law-files/${encodeURIComponent(x.file)}`,pdf=/\.pdf$/i.test(x.file);return `<article class="decision-row"><div class="decision-number">${i+1}</div><div class="decision-name"><h3>${esc(x.title)}</h3><p>${esc(x.description||'ملف قانوني للاطلاع والتنزيل')}</p></div><div class="decision-actions">${pdf?`<a class="view-decision" href="${url}" target="_blank" rel="noopener">عرض القانون</a>`:''}<a class="download-decision" href="${url}" download>تحميل ${pdf?'PDF':'الملف'}</a></div></article>`}).join('')}catch{host.innerHTML='<div class="empty-state"><strong>تعذر تحميل فهرس القوانين</strong></div>'}}init()})();
